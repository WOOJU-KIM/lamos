# 🏛 Lumos 퀀트 트레이딩 시스템: 코드 수정 및 운영 불변 헌법 (Hard Rules)

본 문서는 Lumos 트레이딩 시스템을 유지보수하고 코드를 수정할 때 **어떠한 경우에도 임의로 누락하거나 훼손해서는 안 되는 절대 규칙(Hard Rules)**을 정의합니다. 모든 AI 에이전트와 개발자는 코드를 수정할 때마다 본 규칙을 최우선으로 준수해야 합니다.

---

## ⛔ [절대 금지 사항: Never Break]

1. **임의의 핵심 인터락 누락 금지 (No Side-Effect Regression)**:
   - 사용자가 특정 기능 수정을 요청했을 때, **하이브리드 MoE (GBDT 임계값), 크로스에셋 Veto, SOXX 60분봉 추세 필터, ATR 트레일링 스탑, 양방향 매매(TQQQ/SQQQ)** 등 연관된 핵심 매매 로직을 절대로 건드리거나 생략하지 않는다.
2. **사전 영향도 분석 없는 즉흥 수정 금지**:
   - 코드를 수정하기 전, 변경 대상 함수의 호출 관계와 사이드 이펙트를 완벽히 파악한다.
3. **설정값(Config) 하드코딩 절대 금지**:
   - GBDT 임계값(Threshold) 등 설정값은 오직 `config.py`에만 존재하는 변수이다. 모든 `.py` 파일은 임계값 등의 설정 숫자를 직접 하드코딩해서는 안 되며, 반드시 `config.py`에서 변수로 임포트하여(가져다) 써야 한다. (단, 백테스트 목적의 샌드박스나 새로 생성한 임시 스크립트 파일 내에서 테스트 용도로만 변수를 선언하여 사용하는 것은 예외로 허용한다.)
3. **가짜 수익률(Illusion) 창출 금지**:
   - 시뮬레이션 및 백테스트 진행 시 **왕복 거래 수수료 및 슬리피지는 반드시 최소 0.20% (0.0020) 이상**으로 설정하여 가짜 수익률이 계산되지 않도록 엄격히 통제한다.
4. **무결성 회귀 테스트 미실행 금지**:
   - 코드 수정 후에는 반드시 `python tests/test_system_integrity.py`를 실행하여 핵심 인터락이 정상 동작함을 검증한다.

---

## 🔒 [핵심 6대 매매/운영 불변 룰 (Core Trading Rules)]

### 1. 🎯 진입 3대 인터락 및 하이브리드 MoE 의사결정 절대 기준
- **GBDT 파형 모델 단일 트리거 (임계값 ≥ 60%)**: 오직 15분봉 GBDT 방향 및 확신도(≥60%)가 최우선 조건이다.
- **크로스에셋 Veto 및 GBDT 인자 통합**: 크로스에셋(NVDA, QQQ 등) 데이터는 GBDT 모델의 학습 인자(Feature)로 융합되어 판단의 정확도를 높일 뿐만 아니라, GBDT 신호 방향과 크로스에셋 역풍이 감지될 경우 진입을 엄격히 차단하는 **독립적인 Veto 방패**로도 이중 작동한다.
- **다중 스크린 필터**: SOXX 60분봉 추세(20EMA 기준)와 5분봉 RSI 눌림목(과열권 진입 차단) 조건이 모두 충족되어야 한다.
- *(단, 기존에 존재했던 일일 3-Out 서킷브레이커는 악성 억제기로 판명되어 영구히 폐지되었다.)*
- **학습 데이터 롤링 원칙**: 매주 직전 2년(104주) 데이터를 기반으로 모델을 학습시켜 최신 상태를 유지한다 (백테스트와 실전 매매 동일).

### 2. 🏛 100% 증권사 원장 통신 동기화 (Single Source of Truth)
- 메모리 가상 변수(`_active_position`)로 매도/청산을 판단하지 않는다.
- **오직 키움 증권사 원장에 실제 체결 입고된 주식(poss_qty > 0)이 있을 때만** 매도 청산 로직을 집행한다.
- 매수 미체결 상태에서는 어떠한 청산 시도도 하지 않는다.

### 3. 🛡️ 실전 청산 원칙 (ATR 트레일링 스탑) 및 장 마감 청산
- 5분봉 기준 **ATR 트레일링 스탑**을 사용하여 추세를 극대화한다.
- **목표 익절 (Max TP)**: +3.5%
- **손절 라인 (ATR SL)**: max(2.0%, min(3.2%, ATR(14) * 1.5))
- **트레일링 발동 (Trailing Trigger)**: +2.0% 도달 시 안전수익(+0.5%) 방어선 확보. 이후 고점 대비 -0.8% 하락 시 청산.
- **장 마감 10분 전(15:50 NYT, 04:50 KST)**에만 0% 오버나잇 전량 시장가 청산을 단독 수행한다. (CarryOverClear 금지)

### 4. ⏳ 미체결 스마트 타임아웃 & 단일 포지션 릴레이 원칙
- **단일 포지션 릴레이 (중복 진입 차단)**: 포지션을 보유 중이거나 미체결 상태일 경우, 신규 GBDT 매수 신호가 발생해도 즉각 무시(Jump)하여 수수료 출혈을 막는다.
- **매수 미체결(3초 타임아웃)**: 3초간 미체결 시 자동 취소 ➔ 최신 호가(Ask +0.03$)로 1회 재발주 ➔ 지속 미체결 시 완전 취소하여 예수금 100% 보존 후 즉시 다음 매매 신호 대기.
- **매도 미체결(3초 타임아웃)**: 3초간 미체결 시 자동 취소 ➔ 원장 잔고 재확인 후 최신 호가(Bid -0.05$)로 가격을 낮춰 반드시 100% 전량 청산 완결.

### 5. ⏱️ 투자 진행 필수 체크리스트: 글로벌 시간 동기화
- **투자 가동 전 체크리스트 1번 검증**: 로컬 OS 시계와 뉴욕 거래소 시계(`ZoneInfo("America/New_York")`) 시차 일치 검증 통과 필수.
- **모델 스위칭 상호 배타성 보장**:
  - Main Phase (15m GBDT):  9:30 ~ 15:30 EDT
  - 쿨다운 (신규 진입 차단): 15:30 ~ 15:50 EDT
  - EOD 100% 현금화 청산: 15:50 ~ 16:00 EDT

### 6. 💰 진입 비중 및 슬리피지 방어 (99% Allocation Rule)
- **증거금 부족 에러(Insufficient Funds) 원천 차단**: 매수 주문 시 호가를 올려서(Ask + .03) 시장가처럼 추격 매수를 진행하므로, 가용 예수금(usd_avail)의 100%를 꽉 채워 수량을 계산하면 키움증권 서버에서 증거금 부족으로 주문이 거부될 수 있습니다.
- **적정 비중 99% 고정**: 따라서 진입 수량(`order_qty`)을 계산할 때는 반드시 **가용 USD의 99% (`usd_avail * 0.99`)** 만을 사용하여 1%의 넉넉한 현금 버퍼를 남겨야 합니다.
- **수량 계산 공식**: `order_qty = int((usd_avail * 0.99) / (cur_px + 0.05))`
  *(현재가보다 .05 비싸게 산다고 가정하여 분모를 키움으로써 안전 버퍼를 이중으로 확보)*
- **어떤 에이전트라도 이 99% 룰을 100%나 90% 등으로 임의 변경해서는 절대 안 됩니다.**

---

## 🛠️ [코드 수정 시 필수 워크플로우]

1. **스코프 한정**: 사용자가 요청한 범위 외의 핵심 로직은 절대 건드리지 않는다.
2. **코드 수정 전후 Diff 자체 점검**: 핵심 조건문(`final_conf >= 0.62`, `holdings > 0`, `_manage_unfilled_orders`)이 보존되었는지 확인.
3. **회귀 테스트 필수 실행**: `python tests/test_system_integrity.py` 통과 필수.
